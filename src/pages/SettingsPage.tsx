export function SettingsPage() {
  return (
    <div className="route-page">
      <p className="eyebrow">Nested route</p>
      <h3>Settings</h3>
      <p>
        This route shows how child routes can reuse the same layout while rendering a different
        page body.
      </p>
    </div>
  )
}